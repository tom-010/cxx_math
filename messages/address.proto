syntax = "proto3";

package messages;

// Always import protos with a full path relative to the WORKSPACE file.
import "messages/zip_code.proto";

message Address {
  string city = 1;
  ZipCode zip_code = 2;
}